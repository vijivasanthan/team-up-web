define(
  ['../controllers'],
  function (controllers)
  {
    'use strict';

    controllers.controller(
      'profileEdit',
      function (Member)
      {
        var self = this;
      }
    );
  }
);
